version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: playmate
      POSTGRES_PASSWORD: playmate123
      POSTGRES_DB: playmate_db
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: